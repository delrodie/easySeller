{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, { 'action': path('fos_user_change_password'), 'attr': { 'class': 'form-horizontal m-t-20' } }) }}
    {{ form_widget(form) }}

    <div class="form-group text-center m-t-40">
      <div class="col-xs-12">
        <button class="btn btn-primary btn-block btn-lg waves-effect waves-light" type="submit" id="_submit" name="_submit">{{ 'change_password.submit'|trans }}</button>
      </div>
    </div>
    <!--<div>
        <input type="submit" value="{{ 'change_password.submit'|trans }}" />
    </div>-->
{{ form_end(form) }}
